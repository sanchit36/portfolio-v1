import styled from "styled-components";
import { PRIMARY, WHITE } from "../../globalStyles";

export const HeadingStyled = styled.h2`
  color: ${WHITE};
  margin-bottom: 5rem;
  span {
    color: ${PRIMARY};
  }
`;
